# 트러블슈팅 기록 가이드

트러블슈팅은 작업일지보다 자세히 씁니다. 목적은 “이번 문제를 해결했다”에서 끝나는 것이 아니라, 다음 에이전트가 같은 문제를 만나도 빠르게 판단할 수 있게 만드는 것입니다.

문제가 반복될 가능성이 있거나, 원인 조사에 시간이 들었거나, 외부 도구/GitHub/Notion/Docker/CI와 얽힌 문제라면 Notion 트러블슈팅 DB에 기록합니다.

## 기록 대상

아래 중 하나라도 해당하면 기록합니다.

- CI, Docker, GitHub, Notion, 외부 API, 인증, 환경 변수 때문에 작업이 막힘
- 같은 증상이 다시 나타날 가능성이 있음
- 원인을 찾기까지 10분 이상 조사함
- 해결에 특정 명령, 설정, 순서가 필요했음
- 다른 트랙 작업자가 같은 문제를 만날 수 있음
- 임시 우회나 후속 조치가 남음

단순 오타처럼 바로 고치고 재발 가능성이 낮은 문제는 PR 본문에만 짧게 남겨도 됩니다.

## Notion DB 위치

- Project hub: https://www.notion.so/35fdf321bd89809b87e4fc8eae4c2e77
- 트러블슈팅 DB: https://www.notion.so/35fdf321bd8981559e31e55584337cea

## 속성 작성 기준

| 속성 | 작성 기준 |
| --- | --- |
| `문제` | 증상을 한 문장으로 씁니다. 예: `GitHub PR 본문 한글이 물음표로 깨짐` |
| `상태` | `Open`, `Watching`, `Resolved` |
| `심각도` | 작업 차단은 `High`, 우회 가능은 `Medium`, 참고 수준은 `Low` |
| `영역` | `GitHub`, `Notion`, `Docker`, `CI`, `Backend`, `Pipeline` 중 관련 영역 |
| `원인` | 확정 원인만 씁니다. 추정이면 `추정:`으로 시작합니다 |
| `해결` | 실제로 먹힌 해결 방법을 씁니다 |
| `재발 방지` | 다음에 어떻게 피할지 씁니다 |
| `링크` | 관련 PR, 로그, 문서 중 가장 대표 링크 |
| `발생일` | 문제를 확인한 날짜 |

## 카드 본문 템플릿

Notion 트러블슈팅 카드 본문은 아래 구조를 씁니다.

```md
## 🚨 한눈에 보기

> 한 문장으로 문제와 결론을 요약합니다.

| 항목 | 내용 |
| --- | --- |
| 상태 | Open / Watching / Resolved |
| 심각도 | Low / Medium / High |
| 영역 | GitHub / Notion / Docker / CI / Backend / Pipeline |
| 관련 PR | 링크 |
| 현재 결론 | 확정 원인 또는 아직 모르는 점 |

## 🧭 발생 맥락

- 언제:
- 어떤 작업 중:
- 어떤 환경:
- 직전 변경:

## 🔍 증상

- 관찰된 이상 동작:
- 에러 메시지:
- 재현 조건:
- 영향 범위:

## 🧪 조사 과정

- 확인한 로그:
- 실행한 명령:
- 배제한 원인:
- 새로 알게 된 사실:

## 🧠 원인

- 확정 원인:
- 근거:
- 아직 불확실한 점:

## 🛠️ 해결

- 적용한 해결:
- 바뀐 파일/설정:
- 관련 PR:
- 우회가 있었다면 우회 내용:

## ✅ 검증

- 확인한 명령:
- 통과 기준:
- 실제 결과:

## 🧯 재발 방지

- 추가한 테스트/문서:
- 다음 작업자가 조심할 점:
- 후속 작업:
```

## 작성 원칙

- 원인과 추정을 섞지 않습니다.
- 명령어만 나열하지 않고, 각 명령으로 무엇을 확인했는지 씁니다.
- 해결 전 조사 과정도 남깁니다. 실패한 시도가 다음 사람에게 시간을 아껴줍니다.
- 외부 서비스 제약이면 정책 문구나 에러 메시지를 짧게 남깁니다.
- 개인정보, 토큰, 쿠키, 원문 데이터, 계정 정보는 기록하지 않습니다.

## PR과의 관계

- PR 본문에는 트러블슈팅 링크와 요약만 둡니다.
- 긴 로그와 조사 과정은 Notion 트러블슈팅 DB에 둡니다.
- 해결 PR이 있으면 트러블슈팅 카드의 `링크` 속성에 PR URL을 넣습니다.
- 재발 방지 작업이 남으면 `TASKS.md`나 Notion 다음 작업 DB에 추가합니다.
