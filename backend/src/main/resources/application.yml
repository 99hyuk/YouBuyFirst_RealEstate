spring:
  application:
    name: human-indicator-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/human_indicator?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:human}
    password: ${SPRING_DATASOURCE_PASSWORD:human}
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: ${SERVER_PORT:8080}

app:
  retention:
    post-days: ${POST_RETENTION_DAYS:30}
    cleanup-cron: ${POST_RETENTION_CLEANUP_CRON:0 15 3 * * *}
